# argo-cd chart-integration upstream image allowlist.
#
# The chart's bundled `redis` subchart pulls
# `ecr-public.aws.com/docker/library/redis:8.2.3-alpine`. This is
# declared `unpatchable` in `verity.yaml` because argo-cd hardcodes the
# non-standard ECR Public alias; verity migrated everything else to valkey
# in verity-org/verity#227 but argo-cd is the remaining holdout. So
# chart-gen intentionally leaves it upstream and the image-origin gate
# needs the allowlist entries below.
ecr-public.aws.com/docker/library/redis:
ecr-public.aws.com/docker/library/redis@
