package:
  name: kyverno-init-container-1.16
  version: "1.16.4"
  epoch: 99
  description: "Kyverno init container rebuilt from source"
  copyright:
    - license: Apache-2.0
  dependencies:
    runtime:
      - ca-certificates-bundle

environment:
  contents:
    packages:
      - busybox
      - bash
      - ca-certificates-bundle
      - git
      - go
  environment:
    CGO_ENABLED: "0"
    GO111MODULE: "on"

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/kyverno/kyverno
      tag: v${{package.version}}
  - runs: |
      mkdir -p "${{targets.contextdir}}/usr/bin"
      go build -trimpath -ldflags='-s -w' -o "${{targets.contextdir}}/usr/bin/kyvernopre" ./cmd/kyverno-init

test:
  pipeline:
    - runs: /usr/bin/kyvernopre --help
