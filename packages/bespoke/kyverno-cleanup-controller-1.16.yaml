package:
  name: kyverno-cleanup-controller-1.16
  version: "1.16.4"
  epoch: 99
  description: "Kyverno cleanup controller rebuilt from source"
  copyright:
    - license: Apache-2.0
  dependencies:
    runtime:
      - ca-certificates-bundle

environment:
  contents:
    packages:
      - busybox
      - bash
      - ca-certificates-bundle
      - git
      - go
  environment:
    CGO_ENABLED: "0"
    GO111MODULE: "on"

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/kyverno/kyverno
      tag: v${{package.version}}
  - runs: |
      mkdir -p "${{targets.contextdir}}/usr/bin"
      go build -trimpath -ldflags='-s -w' -o "${{targets.contextdir}}/usr/bin/cleanup-controller" ./cmd/cleanup-controller

test:
  pipeline:
    - runs: /usr/bin/cleanup-controller --help
