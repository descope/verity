{
  "name": "verity-vanity-registry",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "test": "node --test worker.test.mjs",
    "check": "node --check worker.mjs"
  },
  "devDependencies": {
    "wrangler": "4.117.0"
  }
}
