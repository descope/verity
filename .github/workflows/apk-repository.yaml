name: APK Repository (retired)

on:
  workflow_call:

permissions: {}

jobs:
  retired:
    name: Reject standalone APK publication
    runs-on: ubuntu-24.04
    permissions: {}
    steps:
      - name: Fail closed before publication or secret access
        run: exit 1
