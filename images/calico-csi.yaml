name: calico-csi
description: "Calico CSI Driver — Wolfi rebuild"
upstream:
  package: "calico-{{version}}-csi"

types:
  default:
    base: wolfi-base
    packages: ["calico-{{version}}-csi"]
    entrypoint: /usr/bin/csi-driver

versions:
  "3": {}
