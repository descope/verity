name: calico-csi
description: "Calico CSI Driver — Wolfi rebuild"
upstream:
  package: "calico-pod2daemon-{{version}}"

types:
  default:
    base: wolfi-base
    packages: ["calico-pod2daemon-{{version}}"]
    entrypoint: /usr/bin/calico-pod2daemon-csidriver

versions:
  "3.31": {}
