name: spire-controller-manager
description: "SPIRE controller manager (K8s integration) — Wolfi rebuild for zero-CVE base"
upstream:
  package: spire-controller-manager

types:
  default:
    base: wolfi-base
    packages: ["spire-controller-manager"]
    entrypoint: /usr/bin/spire-controller-manager

versions:
  "0": {}
