name: xh
description: "xh — modern HTTP client — Wolfi rebuild for zero-CVE base"
upstream:
  package: xh

types:
  default:
    base: wolfi-base
    packages: ["xh"]
    entrypoint: /usr/bin/xh

versions:
  "0": {}
