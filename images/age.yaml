name: age
description: "age — Modern file encryption — Wolfi rebuild for zero-CVE base"
upstream:
  package: age

types:
  default:
    base: wolfi-base
    packages: ["age"]
    entrypoint: /usr/bin/age

versions:
  "1": {}
