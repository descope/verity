name: kyverno
description: "Kyverno policy engine — Wolfi rebuild replaces Copa-patched distroless"
upstream:
  package: "kyverno-{{version}}"

types:
  default:
    base: wolfi-base
    packages: ["kyverno-{{version}}"]
    entrypoint: /usr/bin/kyverno

versions:
  "1": {}
  "1.17": {}
