name: zoxide
description: "zoxide — smart cd — Wolfi rebuild for zero-CVE base"
upstream:
  package: zoxide

types:
  default:
    base: wolfi-base
    packages: ["zoxide"]
    entrypoint: /usr/bin/zoxide

versions:
  "0": {}
