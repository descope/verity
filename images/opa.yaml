name: opa
description: "Open Policy Agent — Wolfi rebuild for zero-CVE base"
upstream:
  package: opa

types:
  default:
    base: wolfi-base
    packages: ["opa"]
    entrypoint: /usr/bin/opa

versions:
  "1": {}
