name: blackbox-exporter
description: "Prometheus Blackbox Exporter — Copa can't patch scratch-based official image; Wolfi rebuild"
upstream:
  package: prometheus-blackbox-exporter

types:
  default:
    base: wolfi-base
    packages: ["prometheus-blackbox-exporter"]
    entrypoint: /usr/bin/blackbox_exporter

versions:
  "0": {}
