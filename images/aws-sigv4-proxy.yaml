name: aws-sigv4-proxy
description: "AWS SigV4 signing proxy — Wolfi rebuild for zero-CVE base"
upstream:
  package: aws-sigv4-proxy

types:
  default:
    base: wolfi-base
    packages: ["aws-sigv4-proxy"]
    entrypoint: /usr/bin/aws-sigv4-proxy

versions:
  "1": {}
