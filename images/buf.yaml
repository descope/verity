name: buf
description: "buf — Protobuf tooling and registry — Wolfi rebuild for zero-CVE base"
upstream:
  package: buf

types:
  default:
    base: wolfi-base
    packages: ["buf"]
    entrypoint: /usr/bin/buf

versions:
  "1": {}
