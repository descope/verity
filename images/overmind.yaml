name: overmind
description: "Overmind — process manager for Procfile — Wolfi rebuild for zero-CVE base"
upstream:
  package: overmind

types:
  default:
    base: wolfi-base
    packages: ["overmind"]
    entrypoint: /usr/bin/overmind

versions:
  "2": {}
