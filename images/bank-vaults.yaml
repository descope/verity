name: bank-vaults
description: "Bank-Vaults: Vault on Kubernetes — Wolfi rebuild for zero-CVE base"
upstream:
  package: bank-vaults

types:
  default:
    base: wolfi-base
    packages: ["bank-vaults"]
    entrypoint: /usr/bin/bank-vaults

versions:
  "1": {}
