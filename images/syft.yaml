name: syft
description: "Syft — SBOM generator — Wolfi rebuild for zero-CVE base"
upstream:
  package: syft

types:
  default:
    base: wolfi-base
    packages: ["syft"]
    entrypoint: /usr/bin/syft

versions:
  "1": {}
