name: nuclei
description: "Nuclei — fast template-based vulnerability scanner; Wolfi rebuild for zero-CVE base"
upstream:
  package: nuclei

types:
  default:
    base: wolfi-base
    packages: ["nuclei"]
    entrypoint: /usr/bin/nuclei

versions:
  "3": {}
