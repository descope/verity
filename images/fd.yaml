name: fd
description: "fd — fast find replacement — Wolfi rebuild for zero-CVE base"
upstream:
  package: fd

types:
  default:
    base: wolfi-base
    packages: ["fd"]
    entrypoint: /usr/bin/fd

versions:
  "10": {}
