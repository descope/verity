name: crossplane-crank
description: "Crossplane CLI (crank) — Wolfi rebuild for zero-CVE base"
upstream:
  package: crossplane-crank

types:
  default:
    base: wolfi-base
    packages: ["crossplane-crank"]
    entrypoint: /usr/bin/crank

versions:
  "1": {}
