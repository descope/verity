name: kine
description: "Kine — etcd shim for SQL databases — Wolfi rebuild for zero-CVE base"
upstream:
  package: kine

types:
  default:
    base: wolfi-base
    packages: ["kine"]
    entrypoint: /usr/bin/kine

versions:
  "0": {}
