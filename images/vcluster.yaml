name: vcluster
description: "vcluster — virtual Kubernetes clusters inside a namespace; Wolfi rebuild for zero-CVE base"
upstream:
  package: vcluster

types:
  default:
    base: wolfi-base
    packages: ["vcluster"]
    entrypoint: /usr/bin/vcluster

versions:
  "0": {}
