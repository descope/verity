name: wasmtime
description: "Wasmtime WebAssembly runtime — Wolfi rebuild for zero-CVE base"
upstream:
  package: wasmtime

types:
  default:
    base: wolfi-base
    packages: ["wasmtime"]
    entrypoint: /usr/bin/wasmtime

versions:
  "31": {}
