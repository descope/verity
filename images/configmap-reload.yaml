name: configmap-reload
description: "ConfigMap Reload — Wolfi rebuild replaces Copa-patched Debian base"
upstream:
  package: configmap-reload

types:
  default:
    base: wolfi-base
    packages: ["configmap-reload"]
    entrypoint: /usr/bin/configmap-reload

versions:
  "0": {}
