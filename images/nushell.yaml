name: nushell
description: "Nushell modern shell — Wolfi rebuild for zero-CVE base"
upstream:
  package: nushell

types:
  default:
    base: wolfi-base
    packages: ["nushell"]
    entrypoint: /usr/bin/nu

versions:
  "0": {}
