name: htop
description: "htop interactive process viewer — Wolfi rebuild for zero-CVE base"
upstream:
  package: htop

types:
  default:
    base: wolfi-base
    packages: ["htop"]
    entrypoint: /usr/bin/htop

versions:
  "3": {}
