name: difftastic
description: "difftastic — structural diff tool — Wolfi rebuild for zero-CVE base"
upstream:
  package: difftastic

types:
  default:
    base: wolfi-base
    packages: ["difftastic"]
    entrypoint: /usr/bin/difft

versions:
  "0": {}
