name: apisix-ingress-controller
description: "Apache APISIX Ingress Controller — Wolfi rebuild for zero-CVE base"
upstream:
  package: apisix-ingress-controller

types:
  default:
    base: wolfi-base
    packages: ["apisix-ingress-controller"]
    entrypoint: /usr/bin/apisix-ingress-controller

versions:
  "1": {}
