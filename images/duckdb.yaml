name: duckdb
description: "DuckDB analytical SQL CLI — Wolfi rebuild for zero-CVE base"
upstream:
  package: duckdb

types:
  default:
    base: wolfi-base
    packages: ["duckdb"]
    entrypoint: /usr/bin/duckdb

versions:
  "1": {}
