name: aws-node-termination-handler
description: "AWS Node Termination Handler — Wolfi rebuild for zero-CVE base"
upstream:
  package: aws-node-termination-handler

types:
  default:
    base: wolfi-base
    packages: ["aws-node-termination-handler"]
    entrypoint: /usr/bin/node-termination-handler

versions:
  "1": {}
