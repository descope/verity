name: fzf
description: "fzf fuzzy finder — Wolfi rebuild for zero-CVE base"
upstream:
  package: fzf

types:
  default:
    base: wolfi-base
    packages: ["fzf"]
    entrypoint: /usr/bin/fzf

versions:
  "0": {}
