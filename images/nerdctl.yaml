name: nerdctl
description: "nerdctl — Docker-compatible CLI for containerd; Wolfi rebuild for zero-CVE base"
upstream:
  package: nerdctl

types:
  default:
    base: wolfi-base
    packages: ["nerdctl"]
    entrypoint: /usr/bin/nerdctl

versions:
  "2": {}
