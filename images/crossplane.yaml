name: crossplane
description: "Crossplane cloud-native control plane — Wolfi rebuild"
upstream:
  package: "crossplane-{{version}}"

types:
  default:
    base: wolfi-base
    packages: ["crossplane-{{version}}"]
    entrypoint: /usr/bin/crossplane

versions:
  "2": {}
