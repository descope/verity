name: skaffold
description: "Skaffold — K8s dev workflow — Wolfi rebuild for zero-CVE base"
upstream:
  package: skaffold

types:
  default:
    base: wolfi-base
    packages: ["skaffold"]
    entrypoint: /usr/bin/skaffold

versions:
  "2": {}
