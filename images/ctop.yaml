name: ctop
description: "ctop — top-like UI for containers — Wolfi rebuild for zero-CVE base"
upstream:
  package: ctop

types:
  default:
    base: wolfi-base
    packages: ["ctop"]
    entrypoint: /usr/bin/ctop

versions:
  "0": {}
