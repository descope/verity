name: aws-eks-pod-identity-agent
description: "AWS EKS Pod Identity Agent — Wolfi rebuild for zero-CVE base"
upstream:
  package: aws-eks-pod-identity-agent

types:
  default:
    base: wolfi-base
    packages: ["aws-eks-pod-identity-agent"]
    entrypoint: /usr/bin/eks-pod-identity-agent

versions:
  "0": {}
