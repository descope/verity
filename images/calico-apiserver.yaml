name: calico-apiserver
description: "Calico API Server — Wolfi rebuild"
upstream:
  package: "calico-apiserver-{{version}}"

types:
  default:
    base: wolfi-base
    packages: ["calico-apiserver-{{version}}"]
    entrypoint: /usr/bin/calico-apiserver

versions:
  "3.31": {}
