name: calico-apiserver
description: "Calico API Server — Wolfi rebuild"
upstream:
  package: "calico-{{version}}-apiserver"

types:
  default:
    base: wolfi-base
    packages: ["calico-{{version}}-apiserver"]
    entrypoint: /usr/bin/apiserver

versions:
  "3": {}
