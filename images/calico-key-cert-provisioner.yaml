name: calico-key-cert-provisioner
description: "Calico Key Cert Provisioner — Wolfi rebuild"
upstream:
  package: "calico-{{version}}-key-cert-provisioner"

types:
  default:
    base: wolfi-base
    packages: ["calico-{{version}}-key-cert-provisioner"]
    entrypoint: /usr/bin/key-cert-provisioner

versions:
  "3.31": {}
