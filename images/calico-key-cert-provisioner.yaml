name: calico-key-cert-provisioner
description: "Calico Key Cert Provisioner — Wolfi rebuild"
upstream:
  package: "calico-key-cert-provisioner-{{version}}"

types:
  default:
    base: wolfi-base
    packages: ["calico-key-cert-provisioner-{{version}}"]
    entrypoint: /usr/bin/key-cert-provisioner

versions:
  "3.31": {}
