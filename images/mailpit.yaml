name: mailpit
description: "Mailpit — email testing tool — Wolfi rebuild for zero-CVE base"
upstream:
  package: mailpit

types:
  default:
    base: wolfi-base
    packages: ["mailpit"]
    entrypoint: /usr/bin/mailpit

versions:
  "1": {}
