name: strongswan
description: "strongSwan IPsec VPN — Wolfi rebuild for zero-CVE base"
upstream:
  package: strongswan

types:
  default:
    base: wolfi-base
    packages: ["strongswan"]
    entrypoint: /usr/sbin/charon
    paths:
      - {path: /etc/strongswan.d, type: directory, uid: 65532, gid: 65532, permissions: 0o755}

versions:
  "6": {}
