name: kubebuilder
description: "Kubebuilder — Kubernetes operator SDK — Wolfi rebuild for zero-CVE base"
upstream:
  package: kubebuilder

types:
  default:
    base: wolfi-base
    packages: ["kubebuilder"]
    entrypoint: /usr/bin/kubebuilder

versions:
  "4": {}
