name: azure-workload-identity-webhook
description: "Azure Workload Identity webhook — Wolfi rebuild for zero-CVE base"
upstream:
  package: azure-workload-identity-webhook

types:
  default:
    base: wolfi-base
    packages: ["azure-workload-identity-webhook"]
    entrypoint: /usr/bin/manager

versions:
  "1": {}
