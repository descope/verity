name: aws-privateca-issuer
description: "AWS Private CA Issuer for cert-manager — Wolfi rebuild for zero-CVE base"
upstream:
  package: aws-privateca-issuer

types:
  default:
    base: wolfi-base
    packages: ["aws-privateca-issuer"]
    entrypoint: /usr/bin/manager

versions:
  "1": {}
