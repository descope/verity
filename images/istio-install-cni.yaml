name: istio-install-cni
description: "Istio CNI Installer — Wolfi rebuild replaces Copa-patched Ubuntu"
upstream:
  package: "istio-install-cni-{{version}}"

types:
  default:
    base: wolfi-base
    packages: ["istio-install-cni-{{version}}"]
    entrypoint: /usr/bin/install-cni

versions:
  "1": {}
