name: gpsd
description: "GPS service daemon — Wolfi rebuild for zero-CVE base"
upstream:
  package: gpsd

types:
  default:
    base: wolfi-base
    packages: ["gpsd"]
    entrypoint: /usr/bin/gpsd

versions:
  "3": {}
