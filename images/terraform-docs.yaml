name: terraform-docs
description: "terraform-docs documentation generator — Wolfi rebuild for zero-CVE base"
upstream:
  package: terraform-docs

types:
  default:
    base: wolfi-base
    packages: ["terraform-docs"]
    entrypoint: /usr/bin/terraform-docs

versions:
  "0": {}
