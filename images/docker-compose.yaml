name: docker-compose
description: "Docker Compose — Wolfi rebuild for zero-CVE base"
upstream:
  package: docker-compose

types:
  default:
    base: wolfi-base
    packages: ["docker-compose"]
    entrypoint: /usr/bin/docker-compose

versions:
  "2": {}
