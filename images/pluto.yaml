name: pluto
description: "Pluto deprecated K8s API detector — Wolfi rebuild for zero-CVE base"
upstream:
  package: pluto

types:
  default:
    base: wolfi-base
    packages: ["pluto"]
    entrypoint: /usr/bin/pluto

versions:
  "5": {}
