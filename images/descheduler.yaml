name: descheduler
description: "Kubernetes Descheduler — Wolfi rebuild for zero-CVE base"
upstream:
  package: descheduler

types:
  default:
    base: wolfi-base
    packages: ["descheduler"]
    entrypoint: /usr/bin/descheduler

versions:
  "0": {}
