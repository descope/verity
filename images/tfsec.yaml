name: tfsec
description: "tfsec — Terraform security scanner — Wolfi rebuild for zero-CVE base"
upstream:
  package: tfsec

types:
  default:
    base: wolfi-base
    packages: ["tfsec"]
    entrypoint: /usr/bin/tfsec

versions:
  "1": {}
