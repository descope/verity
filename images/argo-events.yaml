name: argo-events
description: "Argo Events event-driven workflow automation — Copa can't patch distroless-based official image; Wolfi rebuild"
upstream:
  package: argo-events

types:
  default:
    base: wolfi-base
    packages: ["argo-events"]
    entrypoint: /usr/bin/argo-events

versions:
  "1": {}
