name: glab
description: "GitLab CLI — Wolfi rebuild for zero-CVE base"
upstream:
  package: glab

types:
  default:
    base: wolfi-base
    packages: ["glab"]
    entrypoint: /usr/bin/glab

versions:
  "1": {}
