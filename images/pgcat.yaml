name: pgcat
description: "pgCat PostgreSQL pooler/proxy — Wolfi rebuild for zero-CVE base"
upstream:
  package: pgcat

types:
  default:
    base: wolfi-base
    packages: ["pgcat"]
    entrypoint: /usr/bin/pgcat

versions:
  "1": {}
