name: pigz
description: "pigz parallel gzip — Wolfi rebuild for zero-CVE base"
upstream:
  package: pigz

types:
  default:
    base: wolfi-base
    packages: ["pigz"]
    entrypoint: /usr/bin/pigz

versions:
  "2": {}
