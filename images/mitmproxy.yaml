name: mitmproxy
description: "mitmproxy interactive HTTPS proxy — Wolfi rebuild for zero-CVE base"
upstream:
  package: mitmproxy

types:
  default:
    base: wolfi-base
    packages: ["mitmproxy"]
    entrypoint: /usr/bin/mitmproxy

versions:
  "9": {}
