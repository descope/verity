name: rebar3
description: "rebar3 Erlang build tool — Wolfi rebuild for zero-CVE base"
upstream:
  package: rebar3

types:
  default:
    base: wolfi-base
    packages: ["rebar3"]
    entrypoint: /usr/bin/rebar3

versions:
  "3": {}
