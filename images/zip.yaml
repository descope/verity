name: zip
description: "zip archiver — Wolfi rebuild for zero-CVE base"
upstream:
  package: zip

types:
  default:
    base: wolfi-base
    packages: ["zip"]
    entrypoint: /usr/bin/zip

versions:
  "3": {}
