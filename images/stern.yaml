name: stern
description: "stern multi-pod log tailer — Wolfi rebuild for zero-CVE CLI image"
upstream:
  package: stern

types:
  default:
    base: wolfi-base
    packages: ["stern"]
    entrypoint: /usr/bin/stern

versions:
  "1": {}
