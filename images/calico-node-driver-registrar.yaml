name: calico-node-driver-registrar
description: "Calico CSI Node Driver Registrar — Wolfi rebuild"
upstream:
  package: "calico-{{version}}-node-driver-registrar"

types:
  default:
    base: wolfi-base
    packages: ["calico-{{version}}-node-driver-registrar"]
    entrypoint: /usr/bin/csi-node-driver-registrar

versions:
  "3.31": {}
