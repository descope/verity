name: otel-cli
description: "otel-cli — OpenTelemetry from shell — Wolfi rebuild for zero-CVE base"
upstream:
  package: otel-cli

types:
  default:
    base: wolfi-base
    packages: ["otel-cli"]
    entrypoint: /usr/bin/otel-cli

versions:
  "0": {}
