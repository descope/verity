name: calico-kube-controllers
description: "Calico Kube Controllers — Wolfi rebuild"
upstream:
  package: "calico-kube-controllers-{{version}}"

types:
  default:
    base: wolfi-base
    packages: ["calico-kube-controllers-{{version}}"]
    entrypoint: /usr/bin/calico-kube-controllers

versions:
  "3.31": {}
