name: calico-kube-controllers
description: "Calico Kube Controllers — Wolfi rebuild"
upstream:
  package: "calico-{{version}}-kube-controllers"

types:
  default:
    base: wolfi-base
    packages: ["calico-{{version}}-kube-controllers"]
    entrypoint: /usr/bin/kube-controllers

versions:
  "3": {}
