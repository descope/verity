name: runc
description: "OCI runc container runtime — Wolfi rebuild for zero-CVE base"
upstream:
  package: runc

types:
  default:
    base: wolfi-base
    packages: ["runc"]
    entrypoint: /usr/bin/runc

versions:
  "1": {}
