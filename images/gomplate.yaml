name: gomplate
description: "Gomplate template renderer — Wolfi rebuild for zero-CVE base"
upstream:
  package: gomplate

types:
  default:
    base: wolfi-base
    packages: ["gomplate"]
    entrypoint: /usr/bin/gomplate

versions:
  "4": {}
