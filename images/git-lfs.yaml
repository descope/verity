name: git-lfs
description: "Git Large File Storage — Wolfi rebuild for zero-CVE base"
upstream:
  package: git-lfs

types:
  default:
    base: wolfi-base
    packages: ["git-lfs"]
    entrypoint: /usr/bin/git-lfs

versions:
  "3": {}
