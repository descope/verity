name: k9s
description: "k9s Kubernetes TUI — Wolfi rebuild for zero-CVE CLI image"
upstream:
  package: k9s

types:
  default:
    base: wolfi-base
    packages: ["k9s"]
    entrypoint: /usr/bin/k9s

versions:
  "0": {}
