name: mage
description: "Mage — Make/Rake-like Go build tool — Wolfi rebuild for zero-CVE base"
upstream:
  package: mage

types:
  default:
    base: wolfi-base
    packages: ["mage"]
    entrypoint: /usr/bin/mage

versions:
  "1": {}
