name: atuin
description: "atuin — Magical shell history — Wolfi rebuild for zero-CVE base"
upstream:
  package: atuin

types:
  default:
    base: wolfi-base
    packages: ["atuin"]
    entrypoint: /usr/bin/atuin

versions:
  "18": {}
