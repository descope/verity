name: terragrunt
description: "Terragrunt — Terraform wrapper — Wolfi rebuild for zero-CVE base"
upstream:
  package: terragrunt

types:
  default:
    base: wolfi-base
    packages: ["terragrunt"]
    entrypoint: /usr/bin/terragrunt

versions:
  "0": {}
