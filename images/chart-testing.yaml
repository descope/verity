name: chart-testing
description: "Helm chart-testing CLI — Wolfi rebuild for zero-CVE base"
upstream:
  package: chart-testing

types:
  default:
    base: wolfi-base
    packages: ["chart-testing"]
    entrypoint: /usr/bin/ct

versions:
  "3": {}
