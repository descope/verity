name: ack
description: "ack — code search — Wolfi rebuild for zero-CVE base"
upstream:
  package: ack

types:
  default:
    base: wolfi-base
    packages: ["ack"]
    entrypoint: /usr/bin/ack

versions:
  "3": {}
