name: blobfuse2
description: "Azure Blob FUSE driver v2 — Wolfi rebuild for zero-CVE base"
upstream:
  package: blobfuse2

types:
  default:
    base: wolfi-base
    packages: ["blobfuse2"]
    entrypoint: /usr/bin/blobfuse2

versions:
  "2": {}
