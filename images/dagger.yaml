name: dagger
description: "Dagger CI/CD pipeline-as-code — Wolfi rebuild for zero-CVE base"
upstream:
  package: dagger

types:
  default:
    base: wolfi-base
    packages: ["dagger"]
    entrypoint: /usr/bin/dagger

versions:
  "0": {}
