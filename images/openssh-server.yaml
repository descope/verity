name: openssh-server
description: "OpenSSH server (sshd) — Wolfi rebuild for zero-CVE base"
upstream:
  package: openssh-server

types:
  default:
    base: wolfi-base
    packages: ["openssh-server"]
    entrypoint: /usr/bin/sshd
    paths:
      - {path: /etc/ssh, type: directory, uid: 65532, gid: 65532, permissions: 0o755}
      - {path: /var/empty, type: directory, uid: 65532, gid: 65532, permissions: 0o755}

versions:
  "10": {}
