name: argo-workflow-controller
description: "Argo Workflows controller (server-side) — Wolfi rebuild for zero-CVE base"
upstream:
  package: argo-workflow-controller

types:
  default:
    base: wolfi-base
    packages: ["argo-workflow-controller"]
    entrypoint: /usr/bin/workflow-controller

versions:
  "3": {}
