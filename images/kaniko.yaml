name: kaniko
description: "Kaniko in-cluster image builder — Copa can't patch scratch-based official image; Wolfi rebuild"
upstream:
  package: kaniko

types:
  default:
    base: wolfi-base
    packages: ["kaniko"]
    entrypoint: /usr/bin/executor

versions:
  "1": {}
