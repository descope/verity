name: lazygit
description: "lazygit terminal UI for git — Wolfi rebuild for zero-CVE base"
upstream:
  package: lazygit

types:
  default:
    base: wolfi-base
    packages: ["lazygit"]
    entrypoint: /usr/bin/lazygit

versions:
  "0": {}
