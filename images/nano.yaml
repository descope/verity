name: nano
description: "GNU nano editor — Wolfi rebuild for zero-CVE base"
upstream:
  package: nano

types:
  default:
    base: wolfi-base
    packages: ["nano"]
    entrypoint: /usr/bin/nano

versions:
  "8": {}
