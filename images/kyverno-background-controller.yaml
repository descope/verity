name: kyverno-background-controller
description: "Kyverno Background Controller — Wolfi rebuild"
upstream:
  package: "kyverno-{{version}}-background-controller"

types:
  default:
    base: wolfi-base
    packages: ["kyverno-{{version}}-background-controller"]
    entrypoint: /usr/bin/background-controller

versions:
  "1.17": {}
