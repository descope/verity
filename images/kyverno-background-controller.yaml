name: kyverno-background-controller
description: "Kyverno Background Controller — Wolfi rebuild"
upstream:
  package: "kyverno-background-controller-{{version}}"

types:
  default:
    base: wolfi-base
    packages: ["kyverno-background-controller-{{version}}"]
    entrypoint: /usr/bin/background-controller

versions:
  "1.17": {}
