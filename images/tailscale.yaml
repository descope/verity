name: tailscale
description: "Tailscale (containerboot entrypoint) — Wolfi rebuild for zero-CVE base"
upstream:
  package: tailscale

types:
  default:
    base: wolfi-base
    packages: ["tailscale"]
    entrypoint: /usr/bin/containerboot

versions:
  "1": {}
