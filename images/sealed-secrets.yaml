name: sealed-secrets
description: "Bitnami Sealed Secrets controller — Copa can't patch distroless-based official image; Wolfi rebuild"
upstream:
  package: sealed-secrets

types:
  default:
    base: wolfi-base
    packages: ["sealed-secrets"]
    entrypoint: /usr/bin/controller

versions:
  "0": {}
