name: sealed-secrets
description: "Bitnami Sealed Secrets controller — family-owned immutable Wolfi rebuild"
upstream:
  package: sealed-secrets

types:
  default:
    base: wolfi-base
    packages: ["sealed-secrets-0"]
    entrypoint: /usr/bin/controller
    cmd: --help
    melange:
      bespoke: sealed-secrets-0.yaml

versions:
  "0": {}
