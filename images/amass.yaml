name: amass
description: "OWASP Amass DNS reconnaissance — Wolfi rebuild for zero-CVE base"
upstream:
  package: amass

types:
  default:
    base: wolfi-base
    packages: ["amass"]
    entrypoint: /usr/bin/amass

versions:
  "4": {}
