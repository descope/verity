name: distroless/static-debian12
description: "Pure Verity static runtime — debian12-compatible tag set"
upstream:
  package: wolfi-baselayout

types:
  default:
    base: verity-static

versions:
  latest:
    latest: true
  nonroot: {}
  debug: {}
  debug-nonroot: {}
