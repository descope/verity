name: distroless/static
description: "Pure Verity static runtime — empty nonroot image for static binaries"
upstream:
  package: wolfi-baselayout

types:
  default:
    base: verity-static

versions:
  latest:
    latest: true
  nonroot: {}
  debug: {}
  debug-nonroot: {}
