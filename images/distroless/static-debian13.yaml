name: distroless/static-debian13
description: "Pure Verity static runtime — debian13-compatible tag set"
upstream:
  package: wolfi-baselayout

types:
  default:
    base: verity-static

versions:
  latest:
    latest: true
  nonroot: {}
  debug: {}
  debug-nonroot: {}
