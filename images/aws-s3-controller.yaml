name: aws-s3-controller
description: "AWS S3 Controller for Kubernetes (ACK) — Wolfi rebuild for zero-CVE base"
upstream:
  package: aws-s3-controller

types:
  default:
    base: wolfi-base
    packages: ["aws-s3-controller"]
    entrypoint: /usr/bin/controller

versions:
  "1": {}
