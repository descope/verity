name: restic
description: "restic backup CLI — Wolfi rebuild for zero-CVE base"
upstream:
  package: restic

types:
  default:
    base: wolfi-base
    packages: ["restic"]
    entrypoint: /usr/bin/restic

versions:
  "0": {}
