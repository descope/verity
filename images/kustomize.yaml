name: kustomize
description: "Kustomize Kubernetes config customization — Wolfi rebuild for zero-CVE CLI image"
upstream:
  package: kustomize

types:
  default:
    base: wolfi-base
    packages: ["kustomize"]
    entrypoint: /usr/bin/kustomize

versions:
  "5": {}
