name: yamllint
description: "yamllint — YAML linter — Wolfi rebuild for zero-CVE base"
upstream:
  package: yamllint

types:
  default:
    base: wolfi-base
    packages: ["yamllint"]
    entrypoint: /usr/bin/yamllint

versions:
  "1": {}
