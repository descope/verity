name: jaeger
description: "Jaeger distributed tracing (all-in-one) — Copa can't patch scratch-based official image; Wolfi rebuild"
upstream:
  package: jaeger-all-in-one

types:
  default:
    base: wolfi-base
    packages: ["jaeger-all-in-one"]
    entrypoint: /usr/bin/all-in-one

versions:
  "1": {}
