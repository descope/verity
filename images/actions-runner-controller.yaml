name: actions-runner-controller
description: "GitHub Actions Runner Controller for Kubernetes — Wolfi rebuild for zero-CVE base"
upstream:
  package: actions-runner-controller

types:
  default:
    base: wolfi-base
    packages: ["actions-runner-controller"]
    entrypoint: /usr/bin/ghalistener

versions:
  "0": {}
