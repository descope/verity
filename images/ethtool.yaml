name: ethtool
description: "ethtool — network device CLI — Wolfi rebuild for zero-CVE base"
upstream:
  package: ethtool

types:
  default:
    base: wolfi-base
    packages: ["ethtool"]
    entrypoint: /usr/bin/ethtool

versions:
  "6": {}
