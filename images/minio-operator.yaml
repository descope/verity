name: minio-operator
description: "MinIO Kubernetes operator — Copa can't patch distroless-based official image; Wolfi rebuild"
upstream:
  package: minio-operator

types:
  default:
    base: wolfi-base
    packages: ["minio-operator"]
    entrypoint: /usr/bin/minio-operator
    melange:
      bespoke: minio-operator.yaml

versions:
  "7": {}
