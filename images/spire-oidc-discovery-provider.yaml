name: spire-oidc-discovery-provider
description: "SPIRE OIDC Discovery Provider — Wolfi rebuild for zero-CVE base"
upstream:
  package: spire-oidc-discovery-provider

types:
  default:
    base: wolfi-base
    packages: ["spire-oidc-discovery-provider"]
    entrypoint: /usr/bin/oidc-discovery-provider

versions:
  "1": {}
