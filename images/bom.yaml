name: bom
description: "Sigstore bom — Software Bill of Materials — Wolfi rebuild for zero-CVE base"
upstream:
  package: bom

types:
  default:
    base: wolfi-base
    packages: ["bom"]
    entrypoint: /usr/bin/bom

versions:
  "0": {}
