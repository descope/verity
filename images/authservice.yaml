name: authservice
description: "authservice OAuth/OIDC for Envoy — Wolfi rebuild for zero-CVE base"
upstream:
  package: authservice

types:
  default:
    base: wolfi-base
    packages: ["authservice"]
    entrypoint: /usr/bin/authservice

versions:
  "1": {}
