name: conftest
description: "Open Policy Agent conftest — Wolfi rebuild for zero-CVE base"
upstream:
  package: conftest

types:
  default:
    base: wolfi-base
    packages: ["conftest"]
    entrypoint: /usr/bin/conftest

versions:
  "0": {}
