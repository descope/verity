name: neovim
description: "Neovim editor — Wolfi rebuild for zero-CVE base"
upstream:
  package: neovim

types:
  default:
    base: wolfi-base
    packages: ["neovim"]
    entrypoint: /usr/bin/nvim

versions:
  "0": {}
