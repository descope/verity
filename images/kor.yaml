name: kor
description: "kor — Kubernetes Orphaned Resource finder — Wolfi rebuild for zero-CVE base"
upstream:
  package: kor

types:
  default:
    base: wolfi-base
    packages: ["kor"]
    entrypoint: /usr/bin/kor
    melange:
      bespoke: kor.yaml

versions:
  "0": {}
