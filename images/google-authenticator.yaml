name: google-authenticator
description: "Google Authenticator PAM module CLI — Wolfi rebuild for zero-CVE base"
upstream:
  package: google-authenticator

types:
  default:
    base: wolfi-base
    packages: ["google-authenticator"]
    entrypoint: /usr/bin/google-authenticator

versions:
  "1": {}
