name: act
description: "act — Run GitHub Actions locally — Wolfi rebuild for zero-CVE base"
upstream:
  package: act

types:
  default:
    base: wolfi-base
    packages: ["act"]
    entrypoint: /usr/bin/act

versions:
  "0": {}
