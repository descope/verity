name: ffmpeg
description: "FFmpeg multimedia framework — Wolfi rebuild for zero-CVE base"
upstream:
  package: ffmpeg

types:
  default:
    base: wolfi-base
    packages: ["ffmpeg"]
    entrypoint: /usr/bin/ffmpeg

versions:
  "7": {}
