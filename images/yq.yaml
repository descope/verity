name: yq
description: "yq YAML processor — Wolfi rebuild for zero-CVE base"
upstream:
  package: yq

types:
  default:
    base: wolfi-base
    packages: ["yq"]
    entrypoint: /usr/bin/yq

versions:
  "4": {}
