name: boring-registry
description: "Boring Terraform Registry — Wolfi rebuild for zero-CVE base"
upstream:
  package: boring-registry

types:
  default:
    base: wolfi-base
    packages: ["boring-registry"]
    entrypoint: /usr/bin/boring-registry

versions:
  "0": {}
