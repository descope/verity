name: bat
description: "bat — Syntax-highlighting cat replacement — Wolfi rebuild for zero-CVE base"
upstream:
  package: bat

types:
  default:
    base: wolfi-base
    packages: ["bat"]
    entrypoint: /usr/bin/bat

versions:
  "0": {}
