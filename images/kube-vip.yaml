name: kube-vip
description: "Kube-VIP virtual IP load-balancer — Wolfi rebuild for zero-CVE base"
upstream:
  package: kube-vip

types:
  default:
    base: wolfi-base
    packages: ["kube-vip"]
    entrypoint: /usr/bin/kube-vip

versions:
  "0": {}
