name: atlantis
description: "Atlantis Terraform pull request automation — Wolfi rebuild for zero-CVE base"
upstream:
  package: atlantis

types:
  default:
    base: wolfi-base
    packages: ["atlantis"]
    entrypoint: /usr/bin/atlantis

versions:
  "0": {}
