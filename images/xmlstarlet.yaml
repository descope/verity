name: xmlstarlet
description: "XMLStarlet — XML CLI tool — Wolfi rebuild for zero-CVE base"
upstream:
  package: xmlstarlet

types:
  default:
    base: wolfi-base
    packages: ["xmlstarlet"]
    entrypoint: /usr/bin/xml

versions:
  "1": {}
