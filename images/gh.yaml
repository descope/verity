name: gh
description: "GitHub CLI — Wolfi rebuild for zero-CVE base"
upstream:
  package: gh

types:
  default:
    base: wolfi-base
    packages: ["gh"]
    entrypoint: /usr/bin/gh

versions:
  "2": {}
