name: chartmuseum
description: "Helm Chart Museum — Wolfi rebuild for zero-CVE base"
upstream:
  package: chartmuseum

types:
  default:
    base: wolfi-base
    packages: ["chartmuseum"]
    entrypoint: /usr/bin/chartmuseum
    paths:
      - {path: /storage, type: directory, uid: 65532, gid: 65532, permissions: 0o755}

versions:
  "0": {}
