name: gosu
description: "gosu — minimal su replacement for containers — Wolfi rebuild for zero-CVE base"
upstream:
  package: gosu

types:
  default:
    base: wolfi-base
    packages: ["gosu"]
    entrypoint: /usr/bin/gosu

versions:
  "1": {}
