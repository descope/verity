name: trufflehog
description: "TruffleHog — find leaked credentials in code, containers, and SaaS; Wolfi rebuild for zero-CVE base"
upstream:
  package: trufflehog

types:
  default:
    base: wolfi-base
    packages: ["trufflehog"]
    entrypoint: /usr/bin/trufflehog
    melange:
      bespoke: trufflehog.yaml

versions:
  "3": {}
