name: asciidoctor
description: "Asciidoctor publishing toolchain — Wolfi rebuild for zero-CVE base"
upstream:
  package: asciidoctor

types:
  default:
    base: wolfi-base
    packages: ["asciidoctor"]
    entrypoint: /usr/bin/asciidoctor

versions:
  "2": {}
