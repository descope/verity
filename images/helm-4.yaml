name: helm-4
description: "Helm 4 — Kubernetes package manager — Wolfi rebuild for zero-CVE base"
upstream:
  package: helm-4

types:
  default:
    base: wolfi-base
    packages: ["helm-4"]
    entrypoint: /usr/bin/helm

versions:
  "4": {}
