name: mc
description: "Midnight Commander text file manager — Wolfi rebuild for zero-CVE base"
upstream:
  package: mc

types:
  default:
    base: wolfi-base
    packages: ["mc"]
    entrypoint: /usr/bin/mc

versions:
  "0": {}
