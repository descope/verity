name: oras
description: "ORAS — OCI registry client — Wolfi rebuild for zero-CVE base"
upstream:
  package: oras

types:
  default:
    base: wolfi-base
    packages: ["oras"]
    entrypoint: /usr/bin/oras

versions:
  "1": {}
