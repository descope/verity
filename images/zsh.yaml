name: zsh
description: "Z shell — Wolfi rebuild for zero-CVE base"
upstream:
  package: zsh

types:
  default:
    base: wolfi-base
    packages: ["zsh"]
    entrypoint: /usr/bin/zsh

versions:
  "5": {}
