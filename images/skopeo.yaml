name: skopeo
description: "Skopeo — OCI image transfer — Wolfi rebuild for zero-CVE base"
upstream:
  package: skopeo

types:
  default:
    base: wolfi-base
    packages: ["skopeo"]
    entrypoint: /usr/bin/skopeo

versions:
  "1": {}
