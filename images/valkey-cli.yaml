name: valkey-cli
description: "Valkey CLI — Wolfi rebuild for zero-CVE base"
upstream:
  package: valkey-cli

types:
  default:
    base: wolfi-base
    packages: ["valkey-cli"]
    entrypoint: /usr/bin/valkey-cli

versions:
  "8": {}
