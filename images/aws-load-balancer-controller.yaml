name: aws-load-balancer-controller
description: "AWS Load Balancer Controller for Kubernetes — Wolfi rebuild for zero-CVE base"
upstream:
  package: aws-load-balancer-controller

types:
  default:
    base: wolfi-base
    packages: ["aws-load-balancer-controller"]
    entrypoint: /usr/bin/controller

versions:
  "2": {}
