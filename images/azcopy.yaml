name: azcopy
description: "Azure azcopy storage data movement — Wolfi rebuild for zero-CVE base"
upstream:
  package: azcopy

types:
  default:
    base: wolfi-base
    packages: ["azcopy"]
    entrypoint: /usr/bin/azcopy

versions:
  "10": {}
