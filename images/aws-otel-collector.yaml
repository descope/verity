name: aws-otel-collector
description: "AWS Distro for OpenTelemetry Collector — Wolfi rebuild for zero-CVE base"
upstream:
  package: aws-otel-collector

types:
  default:
    base: wolfi-base
    packages: ["aws-otel-collector"]
    entrypoint: /usr/bin/awscollector

versions:
  "0": {}
