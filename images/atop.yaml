name: atop
description: "atop system performance monitor — Wolfi rebuild for zero-CVE base"
upstream:
  package: atop

types:
  default:
    base: wolfi-base
    packages: ["atop"]
    entrypoint: /usr/bin/atop

versions:
  "2": {}
