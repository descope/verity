name: bind
description: "ISC BIND DNS server — Wolfi rebuild for zero-CVE base"
upstream:
  package: bind

types:
  default:
    base: wolfi-base
    packages: ["bind"]
    entrypoint: /usr/bin/named

versions:
  "9": {}
