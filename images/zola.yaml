name: zola
description: "Zola — static site generator — Wolfi rebuild for zero-CVE base"
upstream:
  package: zola

types:
  default:
    base: wolfi-base
    packages: ["zola"]
    entrypoint: /usr/bin/zola

versions:
  "0": {}
