name: kyverno-reports-controller
description: "Kyverno Reports Controller — Wolfi rebuild"
upstream:
  package: "kyverno-reports-controller-{{version}}"

types:
  default:
    base: wolfi-base
    packages: ["kyverno-reports-controller-{{version}}"]
    entrypoint: /usr/bin/reports-controller

versions:
  "1.17": {}
