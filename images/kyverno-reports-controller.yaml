name: kyverno-reports-controller
description: "Kyverno Reports Controller — Wolfi rebuild"
upstream:
  package: "kyverno-{{version}}-reports-controller"

types:
  default:
    base: wolfi-base
    packages: ["kyverno-{{version}}-reports-controller"]
    entrypoint: /usr/bin/reports-controller

versions:
  "1": {}
