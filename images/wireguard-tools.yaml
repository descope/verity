name: wireguard-tools
description: "WireGuard userspace tools — Wolfi rebuild for zero-CVE base"
upstream:
  package: wireguard-tools

types:
  default:
    base: wolfi-base
    packages: ["wireguard-tools"]
    entrypoint: /usr/bin/wg

versions:
  "1": {}
