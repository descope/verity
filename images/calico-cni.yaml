name: calico-cni
description: "Calico CNI plugin — Wolfi rebuild replaces Copa-patched Alpine"
upstream:
  package: "calico-cni-{{version}}"

types:
  default:
    base: wolfi-base
    packages: ["calico-cni-{{version}}"]
    entrypoint: /usr/bin/calico

versions:
  "3.31": {}
