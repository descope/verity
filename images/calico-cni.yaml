name: calico-cni
description: "Calico CNI plugin — Wolfi rebuild replaces Copa-patched Alpine"
upstream:
  package: "calico-{{version}}-cni"

types:
  default:
    base: wolfi-base
    packages: ["calico-{{version}}-cni"]
    entrypoint: /usr/bin/calico

versions:
  "3.31": {}
