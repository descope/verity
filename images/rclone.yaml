name: rclone
description: "rclone cloud storage sync — Wolfi rebuild for zero-CVE base"
upstream:
  package: rclone

types:
  default:
    base: wolfi-base
    packages: ["rclone"]
    entrypoint: /usr/bin/rclone

versions:
  "1": {}
