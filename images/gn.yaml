name: gn
description: "gn — meta-build system — Wolfi rebuild for zero-CVE base"
upstream:
  package: gn

types:
  default:
    base: wolfi-base
    packages: ["gn"]
    entrypoint: /usr/bin/gn

versions:
  "0": {}
