name: vexctl
description: "vexctl — VEX (Vulnerability Exploitability eXchange) CLI — Wolfi rebuild for zero-CVE base"
upstream:
  package: vexctl

types:
  default:
    base: wolfi-base
    packages: ["vexctl"]
    entrypoint: /usr/bin/vexctl

versions:
  "0": {}
