name: harbor-cli
description: "Harbor CLI — Wolfi rebuild for zero-CVE base"
upstream:
  package: harbor-cli

types:
  default:
    base: wolfi-base
    packages: ["harbor-cli"]
    entrypoint: /usr/bin/harbor

versions:
  "0": {}
