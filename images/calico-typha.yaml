name: calico-typha
description: "Calico Typha — Wolfi rebuild"
upstream:
  package: "calico-typhad-{{version}}"

types:
  default:
    base: wolfi-base
    packages: ["calico-typhad-{{version}}"]
    entrypoint: /usr/bin/calico-typha

versions:
  "3.31": {}
