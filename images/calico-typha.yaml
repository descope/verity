name: calico-typha
description: "Calico Typha — Wolfi rebuild"
upstream:
  package: "calico-{{version}}-typha"

types:
  default:
    base: wolfi-base
    packages: ["calico-{{version}}-typha"]
    entrypoint: /usr/bin/calico-typha

versions:
  "3.31": {}
