name: strace
description: "strace system call tracer — Wolfi rebuild for zero-CVE base"
upstream:
  package: strace

types:
  default:
    base: wolfi-base
    packages: ["strace"]
    entrypoint: /usr/bin/strace

versions:
  "6": {}
