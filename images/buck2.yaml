name: buck2
description: "Meta Buck2 build system — Wolfi rebuild for zero-CVE base"
upstream:
  package: buck2

types:
  default:
    base: wolfi-base
    packages: ["buck2"]
    entrypoint: /usr/bin/buck2

versions:
  "20250401-r1": {}
