name: kyverno-cli
description: "Kyverno CLI — Wolfi rebuild"
upstream:
  package: "kyverno-{{version}}-cli"

types:
  default:
    base: wolfi-base
    packages: ["kyverno-{{version}}-cli"]
    entrypoint: /usr/bin/kyverno-cli

versions:
  "1.17": {}
