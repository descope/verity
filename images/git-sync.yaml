name: git-sync
description: "git-sync — Kubernetes Git syncer — Wolfi rebuild for zero-CVE base"
upstream:
  package: git-sync

types:
  default:
    base: wolfi-base
    packages: ["git-sync"]
    entrypoint: /usr/bin/git-sync

versions:
  "4": {}
