name: rsync
description: "rsync file synchronizer — Wolfi rebuild for zero-CVE base"
upstream:
  package: rsync

types:
  default:
    base: wolfi-base
    packages: ["rsync"]
    entrypoint: /usr/bin/rsync

versions:
  "3": {}
