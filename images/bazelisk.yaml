name: bazelisk
description: "Bazelisk Bazel launcher — Wolfi rebuild for zero-CVE base"
upstream:
  package: bazelisk

types:
  default:
    base: wolfi-base
    packages: ["bazelisk"]
    entrypoint: /usr/bin/bazelisk

versions:
  "1": {}
