name: biome
description: "Biome JS/TS toolchain — Wolfi rebuild for zero-CVE base"
upstream:
  package: biome

types:
  default:
    base: wolfi-base
    packages: ["biome"]
    entrypoint: /usr/bin/biome

versions:
  "2": {}
