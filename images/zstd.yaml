name: zstd
description: "zstd compression — Wolfi rebuild for zero-CVE base"
upstream:
  package: zstd

types:
  default:
    base: wolfi-base
    packages: ["zstd"]
    entrypoint: /usr/bin/zstd

versions:
  "1": {}
