name: nginx
description: "Nginx web server — Copa leaves unfixable CVEs in Debian nginx; Wolfi rebuild from source"
upstream:
  package: nginx

types:
  default:
    base: wolfi-base
    packages: ["nginx-mainline"]
    entrypoint: /usr/sbin/nginx -g "daemon off;"
    environment:
      NGINX_VERSION: "{{version}}"
    paths:
      - {path: /run, type: directory, uid: 65532, gid: 65532, permissions: 0o755}
      - {path: /var/cache/nginx, type: directory, uid: 65532, gid: 65532, permissions: 0o755}
      - {path: /var/lib/nginx/logs, type: directory, uid: 65532, gid: 65532, permissions: 0o755}
      - {path: /var/lib/nginx/tmp, type: directory, uid: 65532, gid: 65532, permissions: 0o755}
      - {path: /var/log/nginx, type: directory, uid: 65532, gid: 65532, permissions: 0o755}
      - {path: /etc/nginx/conf.d, type: directory, uid: 65532, gid: 65532, permissions: 0o755}
      - {path: /usr/share/nginx/html, type: directory, uid: 65532, gid: 65532, permissions: 0o755}

  fips:
    base: wolfi-fips
    fips-profile: openssl
    packages: ["nginx-mainline", "openssl-provider-fips"]
    entrypoint: /usr/bin/openssl-fips-entrypoint /usr/sbin/nginx -g "daemon off;"
    environment:
      NGINX_VERSION: "{{version}}"
      OPENSSL_MODULES: /usr/lib/ossl-modules
      OPENSSL_CONF: /etc/ssl/openssl-fips.cnf
    melange:
      bespoke: "openssl-provider-fips.yaml"
    paths:
      - {path: /run, type: directory, uid: 65532, gid: 65532, permissions: 0o755}
      - {path: /var/cache/nginx, type: directory, uid: 65532, gid: 65532, permissions: 0o755}
      - {path: /var/lib/nginx/logs, type: directory, uid: 65532, gid: 65532, permissions: 0o755}
      - {path: /var/lib/nginx/tmp, type: directory, uid: 65532, gid: 65532, permissions: 0o755}
      - {path: /var/log/nginx, type: directory, uid: 65532, gid: 65532, permissions: 0o755}
      - {path: /etc/nginx/conf.d, type: directory, uid: 65532, gid: 65532, permissions: 0o755}
      - {path: /usr/share/nginx/html, type: directory, uid: 65532, gid: 65532, permissions: 0o755}

versions:
  "1.28":
    {}
  "1.29": {}
