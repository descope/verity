name: wasmer
description: "Wasmer WebAssembly runtime — Wolfi rebuild for zero-CVE base"
upstream:
  package: wasmer

types:
  default:
    base: wolfi-base
    packages: ["wasmer"]
    entrypoint: /usr/bin/wasmer

versions:
  "6": {}
