name: haproxy-ingress
description: "HAProxy Ingress Controller — Wolfi rebuild replaces Copa-patched Alpine"
upstream:
  package: "haproxy-ingress-{{version}}"

types:
  default:
    base: wolfi-base
    packages: ["haproxy-ingress-{{version}}"]
    entrypoint: /usr/bin/haproxy-ingress-controller

versions:
  "0": {}
