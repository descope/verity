name: whois
description: "whois — domain WHOIS lookup — Wolfi rebuild for zero-CVE base"
upstream:
  package: whois

types:
  default:
    base: wolfi-base
    packages: ["whois"]
    entrypoint: /usr/bin/whois

versions:
  "5": {}
