name: zellij
description: "Zellij terminal multiplexer — Wolfi rebuild for zero-CVE base"
upstream:
  package: zellij

types:
  default:
    base: wolfi-base
    packages: ["zellij"]
    entrypoint: /usr/bin/zellij

versions:
  "0": {}
