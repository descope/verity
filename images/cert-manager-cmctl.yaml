name: cert-manager-cmctl
description: "cert-manager CLI (cmctl) — Wolfi rebuild replaces Copa-patched upstream with zero-CVE base"
upstream:
  package: cert-manager-cmctl

types:
  default:
    base: wolfi-base
    packages: ["cert-manager-cmctl"]
    entrypoint: /usr/bin/cmctl

versions:
  "2": {}
