name: kwok
description: "kwok — Kubernetes WithOut Kubelet; simulates node + pod lifecycle for testing; Wolfi rebuild"
upstream:
  package: kwok

types:
  default:
    base: wolfi-base
    packages: ["kwok", "kwokctl"]
    entrypoint: /usr/bin/kwok

versions:
  "0": {}
