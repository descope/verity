name: starship
description: "Starship cross-shell prompt — Wolfi rebuild for zero-CVE base"
upstream:
  package: starship

types:
  default:
    base: wolfi-base
    packages: ["starship"]
    entrypoint: /usr/bin/starship

versions:
  "1": {}
