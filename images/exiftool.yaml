name: exiftool
description: "ExifTool — EXIF metadata reader/writer — Wolfi rebuild for zero-CVE base"
upstream:
  package: exiftool

types:
  default:
    base: wolfi-base
    packages: ["exiftool", "busybox"]
    entrypoint: /usr/bin/exiftool
    cmd: -ver

versions:
  "13": {}
