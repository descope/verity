name: falco
description: "Falco runtime security (CNCF graduated) — Copa can't patch the upstream image cleanly; Wolfi rebuild"
upstream:
  package: falco

types:
  default:
    base: wolfi-base
    packages: ["falco"]
    entrypoint: /usr/bin/falco

versions:
  "0": {}
