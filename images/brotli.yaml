name: brotli
description: "Brotli compression CLI — Wolfi rebuild for zero-CVE base"
upstream:
  package: brotli

types:
  default:
    base: wolfi-base
    packages: ["brotli"]
    entrypoint: /usr/bin/brotli

versions:
  "1": {}
