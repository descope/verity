name: sshpass
description: "sshpass — non-interactive SSH password — Wolfi rebuild for zero-CVE base"
upstream:
  package: sshpass

types:
  default:
    base: wolfi-base
    packages: ["sshpass"]
    entrypoint: /usr/bin/sshpass

versions:
  "1": {}
