name: nats-server
description: "NATS messaging server — Wolfi rebuild for zero-CVE base"
upstream:
  package: nats-server

types:
  default:
    base: wolfi-base
    packages: ["nats-server"]
    entrypoint: /usr/bin/nats-server

versions:
  "2": {}
