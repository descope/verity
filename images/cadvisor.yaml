name: cadvisor
description: "Google cAdvisor container metrics — Wolfi rebuild for zero-CVE base"
upstream:
  package: cadvisor

types:
  default:
    base: wolfi-base
    packages: ["cadvisor"]
    entrypoint: /usr/bin/cadvisor

versions:
  "0": {}
