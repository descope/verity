name: emacs
description: "GNU Emacs editor — Wolfi rebuild for zero-CVE base"
upstream:
  package: emacs

types:
  default:
    base: wolfi-base
    packages: ["emacs"]
    entrypoint: /usr/bin/emacs

versions:
  "30": {}
