name: jq
description: "jq JSON processor — Wolfi rebuild for zero-CVE base"
upstream:
  package: jq

types:
  default:
    base: wolfi-base
    packages: ["jq"]
    entrypoint: /usr/bin/jq

versions:
  "1": {}

