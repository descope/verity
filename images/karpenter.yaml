name: karpenter
description: "AWS Karpenter Kubernetes node autoscaler — Wolfi rebuild replaces Copa-patched upstream with zero-CVE base"
upstream:
  package: karpenter-1.11

types:
  default:
    base: wolfi-base
    packages: ["karpenter-1.11"]
    entrypoint: /usr/bin/controller

versions:
  "1.11": {}
