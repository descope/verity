name: oauth2-proxy
description: "OAuth2 Proxy authentication — Copa can't patch distroless-based official image; Wolfi rebuild"
upstream:
  package: oauth2-proxy

types:
  default:
    base: wolfi-base
    packages: ["oauth2-proxy"]
    entrypoint: /usr/bin/oauth2-proxy

versions:
  "7": {}
