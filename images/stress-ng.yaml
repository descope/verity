name: stress-ng
description: "stress-ng — system stress tests — Wolfi rebuild for zero-CVE base"
upstream:
  package: stress-ng

types:
  default:
    base: wolfi-base
    packages: ["stress-ng"]
    entrypoint: /usr/bin/stress-ng

versions:
  "0": {}
