name: direnv
description: "direnv environment loader — Wolfi rebuild for zero-CVE base"
upstream:
  package: direnv

types:
  default:
    base: wolfi-base
    packages: ["direnv"]
    entrypoint: /usr/bin/direnv

versions:
  "2": {}
