name: ripgrep
description: "ripgrep — fast grep — Wolfi rebuild for zero-CVE base"
upstream:
  package: ripgrep

types:
  default:
    base: wolfi-base
    packages: ["ripgrep"]
    entrypoint: /usr/bin/rg

versions:
  "14": {}
