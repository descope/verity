name: just
description: "just command runner — Wolfi rebuild for zero-CVE base"
upstream:
  package: just

types:
  default:
    base: wolfi-base
    packages: ["just"]
    entrypoint: /usr/bin/just

versions:
  "1": {}
