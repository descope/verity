name: cilium-cli
description: "Cilium CLI — Wolfi rebuild for zero-CVE base"
upstream:
  package: cilium-cli

types:
  default:
    base: wolfi-base
    packages: ["cilium-cli"]
    entrypoint: /usr/bin/cilium

versions:
  "0": {}
