name: goreleaser
description: "GoReleaser — Go release automation — Wolfi rebuild for zero-CVE base"
upstream:
  package: goreleaser

types:
  default:
    base: wolfi-base
    packages: ["goreleaser"]
    entrypoint: /usr/bin/goreleaser

versions:
  "2": {}
