name: Setup Binaries
description: Prepare non-Verity Go module dependencies used by workflow tooling.

inputs:
  github-token:
    description: GitHub token for API access (retained for caller compatibility)
    required: false
    default: ""

runs:
  using: composite
  steps:
    - name: Download Go module dependencies
      shell: bash
      run: go mod download
